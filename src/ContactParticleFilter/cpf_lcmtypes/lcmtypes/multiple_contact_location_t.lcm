package cpf_lcmtypes;

// For use with Contact Particle Filter algorithm.

struct multiple_contact_location_t
{
  int64_t utime;
  int16_t num_contacts;
  single_contact_filter_estimate_t contacts[num_contacts]; 
}
