package cpf_lcmtypes;

// For use visualizing Contact Particle Filter algorithm.
struct CPF_data_t
{
  int64_t utime;
  int16_t num_particle_sets;
  CPF_particle_set_t particle_sets[num_particle_sets];
}
