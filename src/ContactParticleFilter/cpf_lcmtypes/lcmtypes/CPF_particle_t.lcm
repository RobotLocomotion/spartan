package cpf_lcmtypes;

// For use with Contact Particle Filter algorithm. Encodes a single particle
struct CPF_particle_t
{
  int64_t utime;
  string link_name;
  float contact_location[3];
  float contact_normal[3];
  float contact_force[3];
}
